{
    "rules": {
        "devices": {
            "$deviceId": {
                ".read": "auth != null && root.child('device_owners').child($deviceId).val() === auth.uid",
                ".write": "auth == null || (auth != null && root.child('device_owners').child($deviceId).val() === auth.uid)"
            }
        }
    }
}
